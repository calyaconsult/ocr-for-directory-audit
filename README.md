# Using OCR for a Quick Directory Audit

## The Task
Use an archived screenshot of a directory to see if the contents or modification dates of a directory have changed since the screenshot was taken.

## Prompt
> Provide a nodejs script that reads this CSV from the file 'ocr-data.csv' and compares its items to the filenames and last modification dates of the files in the current directory. The filename of the script will be 'compare-listings.js' For the comparison skip this file and the csv file.

## Generated Node.js script `compare-listings.js`

## Read more in the instructions (EN, DE)
